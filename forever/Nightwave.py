#!/usr/bin/python
import discord
import asyncio
import time
import UpdatedMessage
import NightwaveItem
class Nightwave(UpdatedMessage.UpdatedMessage):

    def __init__(self, message, database):
        super().__init__(message, database)
    def NightwaveItems():
        super().

    async def refresh(self,):